const LIB_EXPOSE = 'libExpose';

const LIB_READY = 'libReady';

const LOAD_STATUS = {
  LOADED: 'loaded',
  UNLOADED: 'unloaded',
  LOADING: 'loading',
};

export { LIB_EXPOSE, LIB_READY, LOAD_STATUS };
